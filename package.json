{
  "name": "claude-code-wechat-channel",
  "version": "0.2.0",
  "description": "Claude Code WeChat 通知插件 — 通过 agent hook 在任务完成后发送微信通知",
  "type": "module",
  "bin": {
    "claude-code-wechat-channel": "./cli.mjs"
  },
  "files": [
    "cli.mjs",
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build wechat-channel.ts --target node --outfile dist/wechat-channel.js && bun build setup.ts --target node --outfile dist/setup.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Johnixr/claude-code-wechat-channel.git"
  },
  "keywords": [
    "claude-code",
    "wechat",
    "mcp",
    "channel",
    "clawbot",
    "ai-agent"
  ]
}
